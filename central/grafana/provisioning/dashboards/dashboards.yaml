apiVersion: 1

providers:
  - name: Logs Overview
    orgId: 1
    folder: Logs
    folderUid: logs
    type: file
    disableDeletion: true
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
